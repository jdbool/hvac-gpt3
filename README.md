# convert.py

Converts .csv from Kaizen to a .jsonl file needed to train a fine-tuned GPT-3 model.

Guide on generating a fine-tuned model with the .jsonl file: https://beta.openai.com/docs/guides/fine-tuning

# test.py

Tests some rows using the completed model and calculates the errors.
